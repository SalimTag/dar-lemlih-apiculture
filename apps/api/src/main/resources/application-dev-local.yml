spring:
  datasource:
    url: jdbc:mysql://localhost:3306/darlemlih?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: dar
    password: lemlih
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/migration-dev
  mail:
    host: localhost
    port: 1025

server:
  port: 8080

app:
  base-url: http://localhost:8080

# Millisecond JWT settings (15 min / 7 days)
jwt:
  access-token-expiration: 900000
  refresh-token-expiration: 604800000
